{
  "name": "motion-view",
  "version": "1.0.20",
  "author": "Team Macro",
  "main": "lib/index.js",
  "scripts": {
    "build": "rm -rf lib; babel src --out-dir lib",
    "watch": "npm run build -- --watch"
  },
  "babel": {
    "presets": [
      "steelbrain"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-steelbrain": "^4.0.2"
  },
  "dependencies": {
    "mobx": "^3.1.9",
    "react": "^15.4.2"
  }
}
